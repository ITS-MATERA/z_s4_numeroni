# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "2.6"
metadata:
  name: zs4numeroni
type: library
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: http://l2vs2slass01.mef.gov.it:8000
          client: "100"
        app:
          name: Z_S4_NUMERONI
          description: Matchcode Numero NI
          package: Z_ACN_S4_COSP_FIORI
          transport: S2SK918451
